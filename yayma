kernel = np.ones((5,5),np.uint8)

yayma = cv2.erode(image,kernel,iterations = -1)
cv2.imshow("Pencere", yayma)

/* kernel = np.ones((5,5),np.uint8)

yayma = cv2.erode(frame,kernel,iterations = -1)
cv2.imshow("Pencere", yayma)  */
